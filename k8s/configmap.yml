# configmap.yml - 현재 설정 유지
apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: sk-team-03
data:
  DB_URL: "jdbc:mariadb://mariadb-1.mariadb:3306/cloud"  # ✅ 이대로 사용
  SPRING_PROFILES_ACTIVE: "k8s"
  SERVER_PORT: "8080"
  SPRING_DATASOURCE_DRIVER_CLASS_NAME: "org.mariadb.jdbc.Driver"